# Stockfish Engine

Place the compiled Stockfish binary in this directory. The self-play scripts expect the engine executable path when starting games.

Clone the official repository and build with:

```bash
git clone https://github.com/official-stockfish/Stockfish.git
cd Stockfish
make build ARCH=x86-64
```
